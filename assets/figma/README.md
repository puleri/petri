# Figma source assets

The approved source is Figma file `3b2xqM0b7hYpHi1pBe1rgL`; archived gameplay frame `79:57` contains the logo, reticle, germ art, and source images.

The automated asset export was blocked after the authenticated Starter-plan Figma MCP quota was exhausted. Do not redraw or replace the archived assets in this directory. Once the quota resets, export the original images and vector layers from node `79:57` here, preserve their returned filenames, then replace the procedural fallback artwork in `scripts/petri_game.gd` with those textures.

`Page 1` was not modified.
